<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <a href="{{ path('app_pizza_home')}}">Accueil</a>
                <a href="{{ path('app_pizza_list')}}">Liste des pizza</a>
                <a href="{{ path('app_pizza_create')}}">nouvelle pizza</a>
                <a href="{{ path('app_basket_display')}}">Mon panier</a>
                <a href="">dashboard</a>
                <a href="">liste des utilisateurs</a>
                <a href="{{ path('app_user_registration')}}">inscription</a>
                <a href="{{ path('app_login')}}">connection</a>
            </nav>
        </header>
        {% block body %}
        
        {% endblock %}
    </body>
</html>
